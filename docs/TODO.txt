========================
PloneSoftwareCenter ToDo
========================
 * Turn project compact listing into a view instead of a pt
 * Make "by-category" work with new listings
 * Clean up code, including removing unused installers, and
   adding resource directories so people can find stuff
 * Add requires.xml so it auto installs plone.contentratings